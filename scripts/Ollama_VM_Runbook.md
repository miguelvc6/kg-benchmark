# Ollama VM Runbook Pointer

The authoritative Ollama VM runbook is:

- [docs-technical/Ollama_VM_Runbook.md](../docs-technical/Ollama_VM_Runbook.md)

This file is intentionally only a pointer so the `scripts/` directory does not carry a stale copy of operational
instructions. The Phase F/G Ollama wrappers default to the full `data/03_world_state.json` artifact. Follow the
technical runbook before running VM jobs.
